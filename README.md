# Colin Yuchen Wang 的个人主页

这是一个无构建依赖的静态个人主页，通过 GitHub Pages 发布到 `colinyuchen.com`。

## 发布

推送到 `main` 分支后，GitHub Actions 会自动发布。首次使用时，需要在仓库的 **Settings → Pages** 中将 **Source** 设为 **GitHub Actions**。

## 域名

仓库中的 `CNAME` 文件将主域名设为 `colinyuchen.com`。DNS 记录需要在阿里云云解析 DNS 中配置。
